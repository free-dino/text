The kernel is an application that always has complete control over everything in the system.

**Host** (CPU): managing and scheduling kernel execution on the GPU
**Device** (GPU): The graphics processor that executes the kernels
